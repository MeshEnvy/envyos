# mcmt-gateway

User-facing notes live in the checkout [`Imperator4422/mcmt-gateway`](https://github.com/Imperator4422/mcmt-gateway) `CHANGELOG.md`. License: GPL-3.0. Distro asset is the `uv` wheel.

Do not duplicate version bullets here. Distro publish reads the checkout file for the pinned tag (`0.1.0`).
