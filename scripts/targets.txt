# EnvyOS OTA build targets — read by scripts/build-mota.sh
#
# Format:  slug  platformio_env  [description…]
#   slug          output dir name under build/<ver>/bench/firmware/<slug>/
#   platformio_env  PlatformIO environment in envycore/
#
# Add a line here to ship a new board/role; rebuild motatool targets if the env set changes.
# otafix BOARD= is inferred from platformio_env by scripts/targets-lib.sh (for build-bl.sh).
#
# Slugs ending in -debug are bench-only (log tail + OTA/admin serial). They are skipped by
# publish/restore. Default `./envyos build firmware` builds field + debug; `--release` or
# `--debug` limits to one set; `--target <slug>` builds explicit slugs.
#
# v0.2.0 ships peer repeaters + companions only. Cache slugs deferred to v0.3.0 (see docs/planned/v0.3.0.md).
# USB folder relay (`OTA_FOLDER_SERIAL`) is bench-only on wismesh-tag-repeater, not field slim builds.

wismesh-tag-repeater   RAK_WisMesh_Tag_repeater              WisMesh Tag repeater (bench USB relay + OTA DUT)
rak4631-repeater-slim   RAK_4631_repeater_slim                RAK4631 slim repeater (no OLED/sensors/BLE)
sensecap-p1pro-repeater-slim  SenseCap_Solar_repeater_slim    SenseCAP Solar P1-Pro slim repeater (OTA)
wismesh-tag-client-ble  RAK_WisMesh_Tag_companion_radio_ble   WisMesh Tag companion (BLE)

wismesh-tag-client-ble-debug       RAK_WisMesh_Tag_companion_radio_ble_debug  WisMesh Tag companion BLE (bench debug)
wismesh-tag-repeater-debug          RAK_WisMesh_Tag_repeater_debug         WisMesh Tag repeater (bench debug)
rak4631-repeater-slim-debug         RAK_4631_repeater_slim_debug           RAK4631 slim repeater (bench debug)
sensecap-p1pro-repeater-slim-debug  SenseCap_Solar_repeater_slim_debug     SenseCAP P1-Pro slim (bench debug)
