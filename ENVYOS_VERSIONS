# EnvyOS component versions (MAJOR.MINOR.PATCH).
# Bench builds use build/<git-branch>/ — not this file's distro= key.
# Set component pins: ./envyos bump patch|minor|major firmware|bootloader|motatool
# Publish chooses git tag v<distro> and writes distro= + firmware= here.
# v0.1.3 hotfix: firmware-only (defer-remote-cli lockup fix); bootloader unchanged.
distro=0.1.3
firmware=0.1.3
bootloader=0.1.2
motatool=0.1.1
